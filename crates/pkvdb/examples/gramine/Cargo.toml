[package]
name = "gramine"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
rusty-leveldb = { path = "../../", features = ["gramine"]}
#rusty-leveldb = { path = "../../"}
cfg-if = "1.0.0"
